# 组件与布局速查 · AI探索者

> 本站已实现的组件速查 + 可扩展方向。做新页面/新内容时从这里选，保持视觉统一。
> 完整版组件库见 esther-design-system 的 `references/components.md` 与 `layouts.md`。

---

## 本站已用组件（都在 assets/style.css 与 index.html 内联样式里）

| 组件 | 类名 | 用在 |
|---|---|---|
| 亮色毛玻璃导航 | `.nav` / `.topbar` | 全站顶部 |
| 终端面板（深色） | `.terminal` `.term-bar` `.term-body` | 首页开场 + 收尾招牌 |
| 白底圆角卡片（顶部彩线） | `.card` + `--accent` | 首页卡片墙（11 张） |
| 三栏编号卡片 | `.pillar`（nth-child 三色线） | 能力区 |
| 白卡作品行 | `.work` | 作品集 |
| 品牌色 CTA 面板 | `.cta`（蓝底白字） | 联系区 |
| 列表卡片 | `ul.list li` | 子页面要点 |
| 文章卡片 | `.article` | 博客列表 |
| 终端引用块 | `.term-quote`（深色面板） | 子页面金句（一页最多 1-2 个） |
| 时间线 | `.tl` `.tl-item` | 成长路径 |
| 按钮 | `.btn` `.btn-green` `.btn-ghost` | 联系/行动 |
| 滚动显现 | `.reveal` `.visible` | 全站入场动效 |

---

## 三色轮换规则

卡片/分区需要区分时按 **蓝 → 黄 → 红** 轮换（`nth-child(3n+1/3n+2/3n)` 或手动 `--accent`）。

---

## 可扩展方向（esther 组件库里有现成代码）

- **引用块**：极简金句（黄短线+蓝结论）、手写批注（虚线+Caveat 标签）、荧光笔高亮、终端风
- **对比列表**：Do/Don't 红蓝对比卡
- **步骤流程**：纵向 Step 线（黄虚线+蓝圆点）、横向 Step 连接线
- **代码面板**：macOS 窗口、笔记本纸、打字机、极简白底
- **书卡**：书籍推荐卡（分类标签+简评）
- **头像集群**：圆形头像 + 虚线环 + 轨道标签

> 原则：一个页面视觉语言统一，同类内容用同一种组件；动效组件一页最多 1-2 个。
